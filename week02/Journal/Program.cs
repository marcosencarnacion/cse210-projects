using System;
using System.Collections.Generic;
using System.IO;

class Program
{
    static void Main(string[] args)
    {
        Journal journal = new Journal(); // Create a new journal object
        PromptGenerator promptGenerator = new PromptGenerator(); // Createa new prompt generator object
    }
}